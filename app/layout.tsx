import type React from "react"
import type { Metadata } from "next"
import { Inter, Playfair_Display } from "next/font/google"
import "./globals.css"

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
})

const playfair = Playfair_Display({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-playfair",
})

export const metadata: Metadata = {
  title: "Rony Fernandes - Insurance, Investment & Real Estate Advisor",
  description:
    "Trusted advisor for insurance, finance & real estate since 1997. Expert in LIC, health insurance, mutual funds, and property investment in Mumbai.",
  keywords:
    "insurance advisor Mumbai, LIC agent, health insurance, mutual funds, real estate advisor, financial planning",
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable} antialiased`}>
      <body className="font-sans">{children}</body>
    </html>
  )
}
